<div class="container-fluid">
  <div class="row">

    <div class="col-2">
  <strong>First name:</strong>
  <%= @user.first_name %>
    </div>

      <div class="col-2">
  <strong>Last name:</strong>
  <%= @user.last_name %>
      </div>

        <div class="col-2">
  <strong>Email:</strong>
  <%= @user.email %>
        </div>
    <div class="col-2">
      <strong>Tasks:</strong>
      <%= @user.tasks.map {|c| c.title}.join(', ') %>
    </div>


<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
  </div>
</div>